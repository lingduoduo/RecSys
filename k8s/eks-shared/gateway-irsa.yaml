# Patch the gateway Deployment to use the IRSA ServiceAccount.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: recsys-api-gateway
  namespace: recsys
spec:
  template:
    spec:
      serviceAccountName: recsys-gateway
